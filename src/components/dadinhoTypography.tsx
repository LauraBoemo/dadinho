import { Typography, TypographyProps } from '@mui/material';
import { styled } from '@mui/material/styles';

export type DadinhoTypographyProps = TypographyProps;

export const DadinhoTypography = styled(Typography)(() => ({}));

export default DadinhoTypography;
