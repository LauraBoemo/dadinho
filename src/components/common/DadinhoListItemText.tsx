import { ListItemText } from "@mui/material";
import { styled } from "@mui/material/styles";

export const DadinhoListItemText = styled(ListItemText)(({ theme }) => ({}));

export default DadinhoListItemText;
