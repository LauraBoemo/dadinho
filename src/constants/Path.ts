export enum PATHS {
    // GENERAL Routes
    LOGIN = "/login",
    REGISTER = "/register",
    CONFIG = "/config",

    // USER Routes
    LEVELS = "/levels",
    ANSWER = "/answer",
    CORRECT = "/correct",
    WRONG = "/wrong",

    // ADMIN Routes
    ADMIN = "/admin",
    HOME = "home",
    ITEMS_CONFIG = "items",
    BASKETS_CONFIG = "baskets",
    LEVELS_CONFIG = "levels",
}